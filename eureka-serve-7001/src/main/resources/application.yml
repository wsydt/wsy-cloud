server:
  port: 7001
spring:
  application:
    name: eureka-serve-7001
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:7002/eureka/
  instance:
    hostname: ${spring.application.name}
