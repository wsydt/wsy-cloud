server:
  port: 7002

spring:
  application:
    name: erueka-serve-${server.port}

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: ${spring.application.name}